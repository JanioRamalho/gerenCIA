# Este arquivo documentará as variáveis quando cada módulo for implementado.
# Não adicione credenciais reais ao repositório.

APP_ENV=development
DATABASE_URL=postgresql+psycopg://gerencia:gerencia@localhost:5432/gerencia
OPENAI_API_KEY=
OPENAI_MODEL=
